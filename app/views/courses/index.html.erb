Classes

<% @courses.each do |course|%>
  <ul>
    <li>
      <%=link_to course.name, user_course_path(:id => course.id)%>
    </li>
  </ul>
<%end%>

<%= link_to 'Create Course', new_user_course_path if policy(@user).new?%>
<%= link_to 'Home Page', root_path%>

